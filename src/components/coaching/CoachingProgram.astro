---
import { coachingModules } from "../../coachingContent";
---

<div id="scroll" class="bg-gray-100 py-8 pb-32">
    <section class="max-w-6xl mx-auto px-8 md:px-16">
        <div class="my-24 flex justify-center">
            <h1 class="text-2xl md:text-3xl relative z-0">
                <span class="absolute w-16 h-16 bg-yellow-200 -z-10 rounded-full -top-3 -left-6"></span>
                Coaching program ☀️
            </h1>
        </div>
        <div>
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                {coachingModules.map((data, index) => 
                    <div class="bg-white rounded-lg shadow-lg p-8">
                        <h3 class="text-[0.7rem] text-gray-800 bg-yellow-300 inline px-1">Session {index + 1}</h3>
                        <h2 class="my-4">{data.name}</h2>
                        <p class="mt-4 text-sm">Theory 🎓</p>
                        <ul>
                            {data.theory?.map((item) => 
                                <li class="text-sm list-disc ml-4 text-gray-700">{item}</li>)}
                        </ul>
                        <p class="mt-4 text-sm">Practice 🛠️</p>
                        <ul>
                            {data.practice?.map((item) => 
                                <li class="text-sm list-disc ml-4 text-gray-700">{item}</li>)}
                        </ul>
                    </div>
                )}
            </div>
        </div>
    </section>
</div>